# chrome-plane extension 环境变量模板
#
# 复制为 .env.development（开发）或 .env.production（生产）。
# Vite 按命令自动选择：npm run dev → development，npm run build → production。
#
# 只有 VITE_ 前缀的变量会暴露给前端，且会被烘焙进构建产物——
# 因此这里只放“后端地址”这类非敏感值，切勿放任何密钥。

VITE_DEFAULT_SERVER_URL=http://localhost:8787
